package com.seafox.nmea_dashboard.ui.widgets.chart

/**
 * High-level chart provider categories used by the chart abstraction layer.
 */
enum class ChartProviderType {
    RASTER_TILES,
    VECTOR_TILES,
    GEOJSON,
    STYLE_JSON,
}
